
TODO
====

This file contains todo-items for the Mercury subproject.

+ **Issues**. There are a number of Mercury issues listed on GitHub. Getting some, many, or all
  of them fixed would be worthwhile!

+ **Generated File Names**. Right now Mercury thinks it is only creating APIs for CubedOS core
  modules, and names files and packages accordingly. This is very limiting since it prevents the
  tool from being used by ordinary application developers! This must be fixed (it's also a
  GitHub issue).

+ **Review/Refresh Generated Code**. The generated code needs updating to reflect the
  multi-domain support architecture CubedOS now uses. It should also be reviewed to follow our
  style guide as closely as possible. One application of Mercury is to do the initial generation
  of an API package, but for that package to be further edited (and then maintained) by hand. In
  that case, highly readable and style-consistent code is a must.
